[tool.uv]
index-url = "https://pypi.tuna.tsinghua.edu.cn/simple"

[project]
name = "CppToolchain"
version = "0.1.0"
description = "C++ 项目常用工具"
requires-python = ">=3.13"
dependencies = [
    "networkx",
    "matplotlib",
]
